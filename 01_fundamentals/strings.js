const firstName = "Saurav";
const lastName = "Verma";
const job = "unemployed";
const birthYear = 1998;

const saurav = `I'm ${firstName} ${lastName} a ${
  2023 - birthYear
} years old ${job} student who is learning to code in javascript and want to become a web developer.`;

console.log(saurav);

console.log(`You can write the
Strings 
in 
Multiline format 
Using Javascript
Template
literals
and it is 
also cleaner.`);
